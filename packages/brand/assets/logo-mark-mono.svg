<!--
  New Manager Success — single-colour mark. Inherits `currentColor`.
  Use where the two-colour mark fails: one-colour print, embossing, a favicon
  mask, or a photographic background. The arrow drops to 60% opacity so the
  letterform still reads when both are the same colour.

  ⚠ PROVISIONAL — see logo-mark.svg.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64" fill="none" role="img" aria-label="New Manager Success">
  <title>New Manager Success</title>
  <path d="M13 56 L22 16 L34 50 L39 33" stroke="currentColor" stroke-width="10.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
  <g opacity="0.6">
    <path d="M33 46 L47 19" stroke="currentColor" stroke-width="9" fill="none" stroke-linecap="round"/>
    <path d="M55 6.5 L37 12 L46.5 19 L44 31 Z" fill="currentColor"/>
  </g>
</svg>
